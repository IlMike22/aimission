<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.michl.aimission">

    <application
        android:name=".Utility.Aimission"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LandingpageScene.Views.LandingpageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GoalDetailScene.Views.GoalDetailActivity" />
        <activity android:name=".GoalsScene.Views.GoalsActivity" />
        <activity android:name=".InfoScene.Views.InfoActivity" />
        <activity android:name=".SettingsScene.Views.SettingsActivity" />
        <activity android:name=".Base.BaseActivity" />
        <activity android:name=".RegisterScene.Views.RegisterActivity"></activity>
    </application>

</manifest>